# Полный список функций и возможностей ПланПро

> **Версия:** 1.0.23  
> **Дата:** 18 февраля 2026  
> **Статус:** Реальный функционал (без выдумок)

---

## 📋 Содержание

- [Глобальные возможности](#глобальные-возможности)
- [Диаграмма Ганта](#диаграмма-ганта)
- [Сетевой график (PERT)](#сетевой-график-pert)
- [Календарное представление](#календарное-представление)
- [Структура декомпозиции работ (WBS)](#структура-декомпозиции-работ-wbs)
- [Гантт отслеживания](#гантт-отслеживания)
- [Лист задач](#лист-задач)
- [Лист ресурсов](#лист-ресурсов)
- [Управление задачами](#управление-задачами)
- [Управление ресурсами](#управление-ресурсами)
- [Работа с проектом](#работа-с-проектом)
- [Горячие клавиши](#горячие-клавиши)

---

## Глобальные возможности

### Основные режимы работы

1. **Диаграмма Ганта** — временная шкала задач с полным редактированием
2. **Сетевой график (PERT)** — визуализация зависимостей между задачами
3. **Календарное представление** — месячный календарь с задачами
4. **Структура декомпозиции работ (WBS)** — иерархическое дерево задач
5. **Гантт отслеживания** — сравнение базового плана с текущим состоянием (только чтение)
6. **Лист задач** — табличное представление всех задач
7. **Лист ресурсов** — табличное представление ресурсов
8. **Отчёты** — система отчётности по проекту

### Работа с файлами

1. Создание нового проекта (Ctrl+N)
2. Открытие проекта из файла .pod (Ctrl+O)
3. Сохранение проекта (Ctrl+S)
4. Сохранение проекта как... (Ctrl+Shift+S)
5. Drag-and-Drop файлов .pod в окно приложения (Windows/macOS)
6. Автосохранение изменений

### Навигация и интерфейс

1. Переключение между представлениями через боковую панель
2. Адаптивный двухуровневый заголовок (TwoTierHeader)
3. Контекстные меню для быстрого доступа к действиям
4. Всплывающие подсказки (tooltips) для всех элементов управления
5. Система помощи с описаниями для каждого представления
6. Тёмная и светлая темы интерфейса (переключение)
7. Поддержка языков: русский, английский

### Системные функции

1. Обработка ошибок с изоляцией по представлениям (ErrorBoundary)
2. Синхронизация состояния между Frontend (TypeScript) и Java Core
3. Проверка совместимости с Windows и Linux
4. Информационные диалоги для ошибок и предупреждений
5. Валидация данных перед сохранением

---

## Диаграмма Ганта

### Отображение задач

1. Визуализация задач на временной шкале
2. Автоматическое отображение суммарных задач (summary tasks)
3. Отображение вех (milestones) в виде ромбов
4. Индикация критического пути красным цветом
5. Отображение прогресса выполнения (progress bar внутри задачи)
6. Цветовая кодировка задач
7. Отображение сегментов задач (split tasks)
8. Иерархическое отображение задач с отступами (уровни)
9. Отображение базового плана (baseline) серым цветом под задачей

### Временная шкала

1. Режимы масштаба: День / Неделя / Месяц
2. Навигация по времени: Назад / Вперед (стрелки)
3. Выбор даты через DatePicker
4. Автоматическая синхронизация с минимальной датой проекта
5. Ограничение навигации диапазоном проекта (minDate/maxDate)
6. Отображение текущей даты (вертикальная линия "сегодня")

### Масштабирование и вид

1. Увеличение масштаба (Zoom In)
2. Уменьшение масштаба (Zoom Out)
3. Сброс масштаба (Fit to Screen)
4. Вертикальная синхронизация скролла между таблицей и диаграммой

### Редактирование задач

1. Добавление новой задачи (кнопка + или Insert)
2. Удаление задачи (контекстное меню или Delete)
3. Изменение длительности задачи перетаскиванием правой границы
4. Перемещение задачи во времени перетаскиванием
5. Изменение прогресса выполнения перетаскиванием handle внутри задачи
6. Двойной клик для открытия диалога свойств задачи

### Иерархия задач

1. Увеличение отступа (Indent) — сделать подзадачей
2. Уменьшение отступа (Outdent) — поднять на уровень выше
3. Автоматическое преобразование в суммарную задачу при добавлении детей
4. Диалог подтверждения при преобразовании в summary task
5. Валидация иерархии (нельзя сделать indent без parent'а выше)

### Связи задач (зависимости)

1. Режим связывания задач (Link Mode)
   - Активация через контекстное меню: "Связать задачи"
   - Выбор задачи-предшественника (predecessor)
   - Визуальная индикация доступных задач
   - Отключение недопустимых связей с объяснением причины
   - Отмена режима связывания (кнопка Cancel или Esc)

2. Отображение связей
   - Линии зависимостей между задачами
   - Цветовая индикация: критический путь красный, обычные серые
   - Стрелки направления зависимости

3. Удаление связей
   - Через контекстное меню: "Разорвать связи"
   - Удаляются все predecessors задачи

4. Типы связей (поддержка)
   - FS (Finish-to-Start) — основной тип
   - Другие типы планируются, но пока FS используется по умолчанию

### Специальные функции

1. **Критический путь (CPM)**
   - Включение/выключение расчёта (кнопка Pulse с иконкой Activity)
   - Визуальное выделение критических задач красным цветом
   - Автоматический пересчёт при изменении задач
   - Индикатор расчёта (анимация Loader2)

2. **Базовый план (Baseline)**
   - Сохранение базового плана (кнопка Pin)
   - Отображение базовых дат серым цветом под задачами
   - Сравнение plan vs actual

3. **Веха (Milestone)**
   - Переключение задачи в веху через контекстное меню
   - Визуализация вехи ромбом вместо полосы
   - Автоматическое нулевое время выполнения

4. **Прерывание задачи (Split Task)**
   - Прерывание задачи на несколько сегментов
   - Диалог выбора даты прерывания и длительности разрыва
   - Объединение сегментов (Merge)
   - Отображение сегментов на диаграмме
   - Включение/выключение функции в настройках

### Контекстное меню задачи

1. Информация о задаче (Task Info) — открыть диалог свойств
2. Сделать вехой / Сделать обычной задачей
3. Связать задачи — включить режим связывания
4. Разорвать связи — удалить все predecessors
5. Прервать задачу — split task (если включено)
6. Объединить сегменты — merge split (если есть сегменты)
7. Увеличить отступ — сделать подзадачей
8. Уменьшить отступ — поднять уровень
9. Переместить вверх
10. Переместить вниз
11. Удалить задачу

### Панель управления (Toolbar)

1. Счётчик задач
2. Переключатель режимов масштаба (День/Неделя/Месяц)
3. Навигация: Назад / Вперёд (со статусом enabled/disabled)
4. Выбор даты (DatePicker с ограничениями min/max)
5. Zoom In / Zoom Out / Reset Zoom
6. Настройки диаграммы (Settings)
7. Сохранить базовый план (Pin)
8. Пульс — критический путь (Activity)

---

## Сетевой график (PERT)

### Визуализация узлов

1. Отображение задач в виде узлов (прямоугольники)
2. Типы узлов:
   - Обычная задача (зелёный)
   - Суммарная задача (синий, с иконкой FolderTree)
   - Веха (ромб, с иконкой Diamond)
3. Индикация критического пути (красная заливка узлов)
4. Отображение прогресса выполнения (progress bar внутри узла)
5. Информация в узле:
   - Название задачи
   - Длительность (X дн.)
   - Даты начала и окончания

### Связи между узлами

1. Отображение линий зависимостей (стрелки)
2. Цветовая индикация: критические связи красные, обычные серые
3. Автоматическое построение путей между узлами

### Иерархия и группировка

1. Отображение иерархии (parent-child)
2. Сворачивание/разворачивание суммарных задач
3. Скрытие дочерних узлов при сворачивании группы
4. Визуальная индикация свёрнутой группы (иконка со стрелкой)

### Интерактивность

1. Перетаскивание узлов (Drag & Drop)
2. Закрепление узлов (isPinned) — фиксация позиции
3. Двойной клик на узле — открыть диалог свойств задачи
4. Создание связей перетаскиванием из одного узла в другой
5. Валидация связей:
   - Запрет связи задачи с самой собой
   - Запрет связывания суммарных задач
   - Запрет циклических зависимостей (parent-child)
   - Диалог с объяснением при блокировке связи

### Масштабирование

1. Увеличение масштаба (Zoom In, кнопка +)
2. Уменьшение масштаба (Zoom Out, кнопка -)
3. Сброс масштаба (кнопка Maximize2)
4. Плавное масштабирование от 0.5x до 2x

### Дополнительные функции

1. Добавление новой задачи (кнопка +)
2. Автоматическая раскладка узлов (Auto Layout) — сброс позиций
3. Footer с подсказками:
   - Легенда: критический путь (красный), обычная задача (зелёный)
   - Подсказка о Drag & Drop

### Диалоги

1. Диалог блокировки связи (ConnectionBlockedDialog):
   - Объяснение причины блокировки
   - Типы причин: self_link, summary_task, parent_child
   - Названия задач, которые пытались связать

---

## Календарное представление

### Отображение календаря

1. Месячная сетка календаря (7 столбцов × строки)
2. Заголовок с днями недели (Пн-Вс)
3. Отображение задач на соответствующих датах
4. Визуализация задач в виде цветных карточек
5. Информация в карточке:
   - Название задачи
   - Цвет задачи (левая граница)
   - Индикатор прогресса
6. Отображение нерабочих дней (серый фон)
7. Выделение текущего дня

### Навигация

1. Переключение месяцев: Назад / Вперёд (стрелки)
2. Быстрый переход на "Сегодня" (кнопка Today)
3. Отображение текущего месяца и года в заголовке

### Интерактивность

1. Клик по задаче — открыть диалог свойств
2. Drag & Drop задач между датами (перемещение во времени)
3. Контекстное меню на задаче (правый клик):
   - Показать свойства
   - Удалить задачу

### Управление задачами

1. Добавление новой задачи (кнопка +)
2. Редактирование задачи (клик или двойной клик)
3. Удаление задачи (контекстное меню)

---

## Структура декомпозиции работ (WBS)

### Визуализация дерева

1. Иерархическое отображение задач в виде дерева
2. Узлы с информацией:
   - Код WBS (автоматическая нумерация: 1, 1.1, 1.1.1)
   - Название задачи
   - Длительность (X дн.)
   - Прогресс выполнения (progress bar)
   - Даты начала и окончания
3. Цветовая индикация:
   - Критические задачи (красная рамка)
   - Обычные задачи (синяя рамка)
   - Суммарные задачи (усиленная рамка)
4. Линии связи между parent и children

### Иерархия

1. Автоматический расчёт кодов WBS
2. Построение иерархии на основе уровней (level)
3. Определение parent-child отношений
4. Сворачивание/разворачивание узлов (клик на узел с детьми)
5. Скрытие дочерних узлов при сворачивании

### Масштабирование

1. Увеличение масштаба (Zoom In, кнопка +)
2. Уменьшение масштаба (Zoom Out, кнопка -)
3. Сброс масштаба (кнопка Maximize)
4. Плавное масштабирование от 0.5x до 2x

### Дополнительные функции

1. Добавление новой задачи (кнопка +)
2. Экспорт СДР в изображение PNG:
   - Диалог выбора пути сохранения
   - Высокое качество (масштаб 2x)
   - Формат PNG с прозрачностью
   - Автоматическое имя файла с датой
3. Выбор узла (клик) — вывод ID в консоль (для отладки)

---

## Гантт отслеживания

### Режим только чтения

1. Отображение задач с базовым планом (baseline)
2. Сравнение плановых и фактических дат
3. Визуальная индикация отклонений
4. Запрет редактирования задач (read-only)

### Панель отслеживания

1. Список задач с информацией:
   - Название
   - Цветовой индикатор
   - Процент выполнения
   - Плановая дата начала (baseline)
2. Фильтрация по завершённости

### Временная шкала

1. Отображение задач на canvas
2. Режим "tracking" для GanttCanvasController
3. Синхронизация начальной даты с диапазоном проекта

### Управление

1. Кнопка "Обновить прогресс" (reserved для будущей реализации)

---

## Лист задач

### Табличное представление

1. Таблица всех задач проекта
2. Колонки (настраиваемые):
   - ID задачи
   - Название
   - Дата начала
   - Дата окончания
   - Длительность
   - Прогресс (%)
   - Ресурсы
   - Предшественники
   - WBS код
   - Уровень (level)
   - Заметки
   - Constraint (ограничение)
   - И другие поля

### Редактирование

1. Inline-редактирование ячеек (клик для редактирования)
2. Валидация введённых данных
3. Автосохранение изменений
4. Обработка конфликтов дат

### Управление задачами

1. Добавление новой задачи (кнопка +)
2. Удаление задачи (контекстное меню или клавиша Delete)
3. Множественный выбор задач (Ctrl+Click, Shift+Click)
4. Массовое удаление выбранных задач

### Экспорт данных

1. Экспорт таблицы в CSV (Excel)
2. Диалог выбора пути сохранения
3. Автоматическое имя файла с датой
4. Сохранение всех колонок таблицы

### Дополнительные функции

1. Контекстное меню на задаче:
   - Показать свойства (диалог)
   - Удалить задачу
2. Диалог свойств задачи (двойной клик или контекстное меню)
3. Сортировка по колонкам (клик на заголовок)
4. Фильтрация задач (поиск)

---

## Лист ресурсов

### Табличное представление

1. Таблица всех ресурсов проекта
2. Колонки (настраиваемые):
   - ID ресурса
   - Название
   - Тип (Work / Material / Cost)
   - Email
   - Телефон
   - Группа
   - Максимальная загрузка (Max Units, %)
   - Стандартная ставка
   - Ставка сверхурочных
   - Стоимость за использование
   - Календарь
   - Код
   - Заметки

### Редактирование

1. Inline-редактирование ячеек
2. Валидация данных ресурсов
3. Автосохранение изменений
4. Конвертация процентов загрузки (units): отображение в % (100%), хранение как коэффициент (1.0)

### Управление ресурсами

1. Добавление нового ресурса (кнопка +)
2. Удаление ресурса (контекстное меню или Delete)
3. Множественный выбор ресурсов
4. Массовое удаление выбранных ресурсов

### Экспорт данных

1. Экспорт таблицы в CSV (Excel)
2. Диалог выбора пути сохранения
3. Автоматическое имя файла с датой

### Управление календарями

1. Кнопка "Календари" — открыть Calendar Manager
2. Просмотр всех календарей проекта
3. Редактирование рабочих часов календаря
4. Создание исключений (праздники, нерабочие дни)
5. Назначение календаря ресурсу

### Дополнительные функции

1. Контекстное меню на ресурсе:
   - Показать информацию
   - Удалить ресурс
2. Справка по вычислениям (кнопка HelpCircle)
3. Валидация календарей при сохранении

---

## Управление задачами

### Создание задачи

1. Через кнопку "Добавить задачу" в любом представлении
2. Через горячую клавишу Insert
3. Автоматическая генерация уникального ID
4. Автоматическое имя "Новая задача X"
5. Значения по умолчанию:
   - Дата начала: сегодня или минимальная дата проекта (настройка)
   - Дата окончания: начало + 1 день
   - Прогресс: 0%
   - Уровень: 1 (корневой)
   - Тип: Fixed Duration
   - Статус: Not Started
   - Приоритет: Medium

### Редактирование задачи

#### Диалог свойств задачи (5 вкладок)

**1. Общие (General)**
- Название задачи
- Тип задачи (Fixed Duration / Fixed Work / Fixed Units)
- Даты: начало / окончание
- Прогресс выполнения (0-100%)
- Приоритет (Low / Medium / High / Critical)
- Статус (Not Started / In Progress / Completed / On Hold / Cancelled)

**2. Ресурсы (Resources)**
- Список доступных ресурсов
- Назначение ресурсов на задачу
- Настройка загрузки (Units) в процентах (50%, 100%, 200%)
- Добавление нескольких ресурсов
- Удаление назначений

**3. Связи (Links)**
- Список предшественников (Predecessors)
- Ввод ID предшественников через запятую
- Список последователей (Successors) — read-only
- Валидация связей при сохранении

**4. Заметки (Notes)**
- Многострочное текстовое поле для заметок
- Без ограничений по длине
- Сохранение вместе с задачей

**5. Дополнительно (Advanced)**
- **Ограничения дат (Constraints):**
  - **Гибкие (Flexible):**
    - ASAP (As Soon As Possible) — как можно раньше
    - ALAP (As Late As Possible) — как можно позже
  - **Полужёсткие (Semi-Rigid):**
    - SNET (Start No Earlier Than) — начало не ранее
    - SNLT (Start No Later Than) — начало не позднее
    - FNET (Finish No Earlier Than) — окончание не ранее
    - FNLT (Finish No Later Than) — окончание не позднее
  - **Жёсткие (Rigid):**
    - MSO (Must Start On) — должна начаться
    - MFO (Must Finish On) — должна завершиться
  
- Дата ограничения (Constraint Date) — если требуется для выбранного constraint
- Индикация категории ограничения (цвет иконки)
- Подсказки для каждого типа ограничения
- Предупреждение, если "Соблюдать обязательные даты" отключено в настройках

### Действия с задачей

1. **Сделать вехой / Сделать обычной задачей**
   - Переключение флага isMilestone
   - Визуальное изменение (ромб vs полоса)

2. **Связывание задач (Link)**
   - Включение режима связывания
   - Выбор предшественника
   - Автоматическое добавление в predecessors
   - Валидация: запрет циклов, summary tasks, self-link

3. **Разрыв связей (Unlink)**
   - Удаление всех predecessors задачи
   - Пересчёт зависимых задач

4. **Прерывание задачи (Split)**
   - Диалог выбора даты прерывания
   - Настройка длительности разрыва (gap)
   - Создание сегментов (TaskSegment[])
   - Отображение сегментов на Gantt

5. **Объединение сегментов (Merge)**
   - Объединение всех сегментов задачи в один
   - Удаление разрывов

6. **Изменение иерархии**
   - **Indent** — сделать подзадачей (увеличить level)
   - **Outdent** — поднять уровень (уменьшить level)
   - Автоматическое преобразование parent в summary task
   - Диалог подтверждения при создании summary

7. **Перемещение в списке**
   - Move Up — переместить вверх в списке
   - Move Down — переместить вниз в списке
   - Сохранение иерархии

8. **Удаление задачи**
   - Проверка разрешения на удаление (настройка)
   - Удаление зависимых связей
   - Пересчёт summary tasks

### Автоматические расчёты

1. **Длительность**
   - Автоматический расчёт из startDate и endDate
   - Учёт рабочих дней календаря
   - Пересчёт при изменении дат

2. **Суммарные задачи (Summary Tasks)**
   - Автоматический расчёт дат: min(children.start) → max(children.end)
   - Агрегация прогресса детей (взвешенный по длительности)
   - Пересчёт при изменении детей

3. **Критический путь (CPM)**
   - Метод критического пути (PERT/CPM алгоритм)
   - Расчёт Early Start / Early Finish
   - Расчёт Late Start / Late Finish
   - Расчёт Total Slack / Free Slack
   - Определение критических задач (slack = 0)
   - Пометка критического пути флагом isCritical

4. **Зависимости**
   - Автоматический сдвиг successor при изменении predecessor
   - Учёт constraint при планировании
   - Валидация циклических зависимостей

### Валидация и конфликты

1. **Конфликт дат с constraint**
   - Диалог предупреждения при нарушении constraint
   - Выбор: принять изменение / отменить
   - Диаграмма конфликта (DependencyConflictDiagram)

2. **Конфликт зависимостей**
   - Проверка перед созданием связи
   - Диалог с объяснением проблемы
   - Блокировка недопустимых связей

---

## Управление ресурсами

### Создание ресурса

1. Через кнопку "Добавить ресурс" в листе ресурсов
2. Автоматическая генерация уникального ID
3. Автоматическое имя "Ресурс X"
4. Значения по умолчанию:
   - Тип: Work (рабочий)
   - Max Units: 100%
   - Календарь: Standard (из настроек проекта)
   - Email: пустой
   - Группа: General

### Редактирование ресурса

#### Диалог информации о ресурсе

1. **Основные свойства:**
   - Название
   - Тип (Work / Material / Cost)
   - Email
   - Телефон
   - Группа

2. **Доступность:**
   - Max Units (максимальная загрузка в процентах)
   - Конвертация: отображение 100%, хранение 1.0

3. **Стоимость:**
   - Стандартная ставка (Standard Rate)
   - Ставка сверхурочных (Overtime Rate)
   - Стоимость за использование (Cost Per Use)

4. **Календарь:**
   - Выбор календаря из списка проектных календарей
   - Валидация существования календаря
   - Fallback на Standard при удалении календаря

5. **Дополнительно:**
   - Код ресурса
   - Заметки

### Назначение ресурсов на задачи

1. Через вкладку "Ресурсы" в диалоге свойств задачи
2. Выбор ресурса из списка доступных
3. Настройка загрузки (Units) в процентах:
   - 50% — половина рабочего времени
   - 100% — полная загрузка
   - 200% — двойная загрузка (2 сотрудника)
4. Добавление нескольких ресурсов на одну задачу
5. Удаление назначения

### Управление календарями

#### Calendar Manager

1. **Просмотр календарей:**
   - Список всех календарей проекта
   - Standard (стандартный рабочий календарь)
   - Пользовательские календари

2. **Редактирование рабочего времени:**
   - Настройка рабочих дней (Пн-Пт)
   - Настройка рабочих часов (например, 9:00-18:00)
   - Перерывы (например, 13:00-14:00)

3. **Исключения (Exceptions):**
   - Добавление праздников
   - Добавление нерабочих дней
   - Настройка специальных дней (например, сокращённый день)
   - Диапазоны дат исключений

4. **Назначение календарей:**
   - На ресурсы (индивидуальный календарь)
   - На проект (календарь по умолчанию)

### Удаление ресурсов

1. Выбор ресурса в листе
2. Удаление через контекстное меню или Delete
3. Удаление всех назначений ресурса из задач
4. Множественное удаление (выбор нескольких ресурсов)

---

## Работа с проектом

### Информация о проекте

1. **Диалог "О проекте" (ProjectInformationDialog):**
   - Название проекта
   - Менеджер проекта (ручное назначение)
   - Дата начала проекта
   - Дата окончания проекта (автоматическая)
   - Режим планирования:
     - Schedule from Start (прямое планирование)
     - Schedule from End (обратное планирование)
   - Жёсткий дедлайн (Must Finish By) — опционально

2. **Статистика проекта (ProjectStatisticsDialog):**
   - Общее количество задач
   - Количество завершённых задач
   - Количество ресурсов
   - Общая длительность проекта
   - Процент выполнения проекта
   - Критический путь (длина в днях)

### Базовые планы (Baselines)

1. **Создание baseline:**
   - Кнопка "Зафиксировать план" (Pin) в Gantt Toolbar
   - Диалог BaselineDialog для выбора имени
   - Сохранение снимка всех дат задач
   - Хранение в массиве baselines

2. **Отображение baseline:**
   - Серая полоса под задачей на Gantt
   - Визуальное сравнение план vs факт

3. **Управление baselines:**
   - Выбор активного baseline
   - Удаление baseline
   - Переключение между baselines

### Настройки проекта

#### Опции проекта (ProjectOptionsDialog)

1. **Календарные настройки:**
   - Часы в рабочем дне (8, 10, 12)
   - Часы в рабочей неделе (40, 45, 50)
   - Дней в месяце (20, 21, 22)
   - Календарь по умолчанию

2. **Планирование:**
   - Соблюдать обязательные даты (Honor Required Dates)
   - Новые задачи начинаются сегодня
   - Автоматическое связывание вставляемых задач

3. **Редактирование:**
   - Разрешить прерывание задач (Split Tasks)
   - Разрешить удаление задач

4. **Расчёты:**
   - Автоматический расчёт критического пути
   - Задержка пересчёта (debounce time)

#### Опции диаграммы Ганта (GanttChartOptionsDialog)

1. **Отображение:**
   - Показывать критический путь
   - Показывать базовый план
   - Показывать slack (резерв времени)
   - Цвет критического пути
   - Цвет обычных задач

2. **Временная шкала:**
   - Режим по умолчанию (День/Неделя/Месяц)
   - Формат дат
   - Первый день недели (Пн/Вс)

#### Опции расчётов (CalculationOptionsDialog)

1. **CPM Engine:**
   - Алгоритм расчёта (Forward/Backward)
   - Учёт календарей
   - Режим обработки summary tasks

2. **Производительность:**
   - Включить кэширование
   - Debounce для автопересчёта

### Импорт и экспорт

1. **Импорт:**
   - Открытие .pod файлов (ProjectLibre формат)
   - Drag-and-Drop .pod файлов в окно (Windows/macOS)
   - Парсинг через Java Core

2. **Экспорт:**
   - Сохранение в .pod формат
   - Экспорт CSV (из листов задач/ресурсов)
   - Экспорт WBS в PNG изображение

### Поиск и фильтрация

1. **Поиск задач (Find Dialog):**
   - Поиск по названию
   - Поиск по ID
   - Поиск по ресурсам
   - Регистронезависимый поиск
   - Переход к найденной задаче

2. **Расширенный поиск (AdvancedSearchDialog):**
   - Множественные критерии
   - Фильтр по датам
   - Фильтр по прогрессу
   - Фильтр по статусу
   - Фильтр по приоритету

3. **Перейти к задаче (GoToDialog):**
   - Ввод ID задачи
   - Быстрый переход и выделение

### Отмена и повтор действий

1. Отмена последнего действия (Ctrl+Z) — в разработке
2. Повтор отменённого действия (Ctrl+Y) — в разработке

---

## Горячие клавиши

### Работа с файлами

| Клавиша | Действие |
|---------|----------|
| Ctrl+N | Создать новый проект |
| Ctrl+O | Открыть проект |
| Ctrl+S | Сохранить проект |
| Ctrl+Shift+S | Сохранить проект как... |
| Ctrl+P | Печать (reserved) |
| Alt+F4 / Ctrl+Q | Выход из приложения |

### Редактирование задач

| Клавиша | Действие |
|---------|----------|
| Insert | Вставить новую задачу |
| Delete | Удалить задачу (если разрешено) |
| Tab | Увеличить отступ (Indent) |
| Shift+Tab | Уменьшить отступ (Outdent) |
| Ctrl+X | Вырезать (reserved) |
| Ctrl+C | Копировать (reserved) |
| Ctrl+V | Вставить (reserved) |
| Ctrl+Z | Отменить (reserved) |
| Ctrl+Y | Повторить (reserved) |

### Навигация

| Клавиша | Действие |
|---------|----------|
| Ctrl+F | Найти задачу |
| Ctrl+G | Перейти к задаче |
| Ctrl+Home | В начало списка |
| Ctrl+End | В конец списка |
| Page Up | Страница вверх |
| Page Down | Страница вниз |

### Представления

| Клавиша | Действие |
|---------|----------|
| Ctrl+Plus | Увеличить масштаб |
| Ctrl+Minus | Уменьшить масштаб |
| Ctrl+0 | Сбросить масштаб |

### Информация

| Клавиша | Действие |
|---------|----------|
| Shift+F2 | Информация о задаче |
| Shift+F3 | Назначить ресурсы |
| Shift+F4 | Информация о ресурсе |
| F1 | Справка (reserved) |

---

## Примечания

### Статус функций

- ✅ **Реализовано** — функция полностью работает
- 🔧 **В разработке** — функция частично реализована
- 📝 **Reserved** — функция запланирована, но не реализована

### Отключённые функции

1. **Дедлайн проекта (Project Deadline)** — функция существует в коде, но временно отключена в интерфейсе (кнопка закомментирована в GanttToolbar)

2. **Отмена/Повтор (Undo/Redo)** — горячие клавиши зарегистрированы, но обработчики пока не реализованы

3. **Печать (Print)** — горячая клавиша зарегистрирована, функция в планах

### Ограничения

1. **Drag-and-Drop на Linux** — не работает из-за ограничений XDND протокола. Используйте кнопку "Открыть проект" в меню.

2. **Типы связей** — пока поддерживается только FS (Finish-to-Start). Другие типы (SS, FF, SF) планируются.

3. **Календари** — при удалении календаря, назначенного ресурсу, происходит автоматическая санитизация на Standard календарь.

---

**Документ составлен на основе анализа исходного кода ПланПро версии 1.0.23**  
**Дата: 18 февраля 2026**

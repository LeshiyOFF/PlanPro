# 🚀 Скрипты сборки ProjectLibre (Isolated Pipeline)

Эта папка содержит профессиональную систему сборки приложения ProjectLibre, построенную на принципах **полной изоляции** и **воспроизводимости**.

## 📋 Обзор архитектуры

Система сборки разделена на три независимых этапа, которые синхронизируются через общую структуру директорий. Основной целевой директорией теперь является **`dist-app`**.

| Этап | Скрипт | Описание | Результат |
| :--- | :--- | :--- | :--- |
| **1. Frontend** | `build-frontend-clean.ps1` | Сборка React (Vite + TS) в изолированном Node.js | `dist-app/` (статические файлы) |
| **2. Java** | `build-java.ps1` | Сборка Java Core через Ant | `projectlibre_build/dist/*.jar` |
| **3. Electron** | `build-electron.ps1` | Компиляция TS и упаковка в `.exe` | `release/` (инсталлятор и распакованная версия) |

---

## 🚀 Быстрый старт (Полная сборка)

Для сборки готового приложения «с нуля» выполните последовательно:

```powershell
# 1. Собрать фронтенд
.\build-scripts\build-frontend-clean.ps1

# 2. Собрать Java-компоненты
.\build-scripts\build-java.ps1

# 3. Упаковать в Electron приложение
.\build-scripts\build-electron.ps1
```

Финальный результат будет находиться в папке **`release/`**.

---

## ⚙️ Параметризация и гибкость

Все скрипты поддерживают общие параметры для переключения между новой и старой структурами проекта:

| Параметр | Тип | Описание | По умолчанию |
| :--- | :--- | :--- | :--- |
| `-DistStructure` | `string` | Имя целевой папки сборки | `dist-app` |
| `-UseLegacyStructure` | `switch` | Если включен, принудительно использует старую папку `dist` | `false` |
| `-ProjectRoot` | `string` | Путь к корню проекта (если запускается не из корня) | Автоопределение |

**Пример использования старой структуры:**
```powershell
.\build-scripts\build-frontend-clean.ps1 -UseLegacyStructure
```

---

## 🛠 Ключевые особенности

1.  **Изоляция среды:** Скрипты используют локальные инструменты (`tools/nodejs`, `tools/apache-ant`), не загрязняя вашу систему глобальными зависимостями.
2.  **Атомарные операции:** Копирование артефактов происходит атомарно. Если сборка прервется, старые рабочие файлы не будут повреждены.
3.  **Автоматическая очистка:** Перед сборкой скрипты находят и останавливают блокирующие процессы (node, java, electron), предотвращая ошибки доступа к файлам.
4.  **Синхронизация с Release:** Для удобства разработки `build-java.ps1` автоматически дублирует JAR-файлы в `release/java/lib`.

---

## 🐛 Отладка

Если после сборки приложение не запускается или показывает белый экран:

1.  **Проверьте папку `dist-app`**: В ней должны быть `index.html` и папка `electron/` с файлом `main.js`.
2.  **Проверьте `package.json`**: Поле `"main"` должно указывать на `dist-app/electron/main.js`.
3.  **Логи сборки**: Каждый скрипт выводит детальный лог выполнения с временными метками. Ищите строки с пометкой `[ERROR]`.

---

**Последнее обновление:** Январь 2026  
**Статус:** Актуально для архитектуры v7.0+

import React from 'react'

/**
 * Ранее запускал модальный Welcome при старте.
 * С Фазой 2 ROADMAP стартовый экран — полноэкранный (FullScreenStartupScreen),
 * показывается через StartupGate в App. Данный компонент больше не открывает диалог.
 */
export const StartupDialogLauncher: React.FC = () => {
  return null
}

